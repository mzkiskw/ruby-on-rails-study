<h1>Task list</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task %>
    </li>
  <% end %>
</ul>

<a href="<%= root_path %>">HOME</a>
<a href="<%= new_task_path %>">create</a>
